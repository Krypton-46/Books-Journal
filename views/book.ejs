<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title><%= locals.book ? locals.book.title : 'Book Details' %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      body {
        font-family: Arial, sans-serif;
        background: #fafafa;
        color: #333;
        padding: 40px;
        max-width: 600px;
        margin: auto;
        line-height: 1.6;
      }

      h1 {
        margin-bottom: 10px;
      }

      .label {
        font-weight: bold;
      }

      .section {
        margin-bottom: 20px;
      }

      .notes {
        background: #f2f2f2;
        padding: 10px;
        border-left: 4px solid #ccc;
        white-space: pre-wrap;
      }

      textarea {
        width: 100%;
        height: 100px;
        padding: 8px;
        margin-top: 5px;
        margin-bottom: 10px;
        border-radius: 4px;
        border: 1px solid #ccc;
        font-size: 1rem;
      }

      button {
        background: #4caf50;
        color: white;
        padding: 10px 16px;
        border: none;
        border-radius: 4px;
        font-size: 1rem;
        cursor: pointer;
      }

      button:hover {
        background: #45a049;
      }
    </style>
  </head>
  <body>
    <% if (locals.book) { %>
    <h1><%= locals.book.title %></h1>
    <div class="book-cover">
      <img src="<%= book.cover_url %>" alt="Book Cover" />
    </div>
    <div class="section">
      <span class="label">Rating:</span> <%= book.rating %>
    </div>

    <div class="section"><span class="label">ISBN:</span> <%=book.isbn %></div>

    <div class="section">
      <span class="label">Date Read:</span>
      <%= book.date_read.toISOString().slice(0,10) %>
    </div>

    <!-- Edit Notes Form -->

    <% if (locals.book.notes && locals.book.notes.trim()) { %>
    <div class="section">
      <span class="label">Current Notes:</span>
      <div class="notes"><%= locals.book.notes %></div>
    </div>
    <% } %> <% } else { %>
    <p>No book data available.</p>
    <% } %>
    <div class="section">
      <form action="/updatenotes" method="POST">
        <!-- Pass ISBN -->
        <input type="hidden" name="isbn" value="<%= book.isbn %>" />

        <label class="label" for="notes">Edit Notes:</label>
        <!-- Pass current notes in textarea -->
        <textarea style="resize: none" id="notes" name="notes">
<%= book.notes || '' %></textarea
        >

        <button type="submit">Update Notes</button>
      </form>
    </div>
  </body>
</html>
