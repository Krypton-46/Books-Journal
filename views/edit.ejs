<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>
      Edit Book - <%= locals.book ? locals.book.title : 'Book Details' %>
    </title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      body {
        font-family: Arial, sans-serif;
        background: #fafafa;
        color: #333;
        padding: 40px;
        max-width: 600px;
        margin: auto;
        line-height: 1.6;
      }

      h1 {
        margin-bottom: 20px;
      }

      label {
        display: block;
        margin-top: 10px;
        font-weight: bold;
      }

      input[type="text"],
      input[type="date"],
      input[type="number"],
      textarea {
        width: 100%;
        padding: 8px;
        margin-top: 5px;
        border: 1px solid #ccc;
        border-radius: 4px;
        font-size: 1rem;
        box-sizing: border-box;
      }

      textarea {
        height: 100px;
        resize: none;
      }

      button {
        background: #4caf50;
        color: white;
        padding: 10px 16px;
        border: none;
        border-radius: 4px;
        font-size: 1rem;
        margin-top: 20px;
        cursor: pointer;
      }

      button:hover {
        background: #45a049;
      }

      .delete-button {
        position: absolute;
        top: 20px;
        right: 40px;
        background: #ff4d4d;
        color: white;
        padding: 8px 12px;
        border-radius: 4px;
        text-decoration: none;
        font-size: 0.9rem;
      }

      .delete-button:hover {
        background: #e60000;
      }

      .book-cover {
        text-align: center;
        margin-bottom: 20px;
      }

      .book-cover img {
        max-width: 100%;
        height: auto;
        border-radius: 4px;
      }
    </style>
  </head>
  <body>
    <% if (locals.book) { %>

    <h1>Edit Book Info</h1>

    <div class="book-cover">
      <img src="<%= book.cover_url %>" alt="Book Cover" />
    </div>

    <form action="/updatebook" method="POST">
      <!-- Hidden field to identify the book -->
      <input type="hidden" name="original_isbn" value="<%= book.isbn %>" />

      <label for="title">Title:</label>
      <input
        type="text"
        id="title"
        name="title"
        value="<%= book.title %>"
        required
      />

      <label for="rating">Rating (1-5):</label>
      <input
        type="number"
        id="rating"
        name="rating"
        min="1"
        max="5"
        step="0.1"
        value="<%= book.rating %>"
      />

      <label for="isbn">ISBN:</label>
      <input
        type="text"
        id="isbn"
        name="isbn"
        value="<%= book.isbn %>"
        required
      />

      <label for="date_read">Date Read:</label>
      <input
        type="date"
        id="date_read"
        name="date_read"
        value="<%= book.date_read.toISOString().slice(0, 10) %>"
      />

      <label for="notes">Notes:</label>
      <textarea id="notes" name="notes"><%= book.notes || '' %></textarea>

      <button type="submit">Save Changes</button>
    </form>
    <% } else { %>
    <p>No book data available.</p>
    <% } %>
  </body>
</html>
